# 🛡️ KampusVPN

Десктопное VPN-приложение на базе sing-box с поддержкой подписок и WireGuard для корпоративных сетей.

## ✨ Возможности

- **Подписки и прямые ссылки** — VLESS, Trojan, Shadowsocks, VMess
- **WireGuard для рабочих сетей** — до 20 корпоративных VPN-туннелей
- **Умная маршрутизация**:
  - 🇷🇺 Российские сайты → Напрямую (без VPN)
  - 🏢 Корпоративные сети → WireGuard
  - 🌍 Остальной трафик → VPN-прокси
- **Автовыбор сервера** — URL-тест для оптимальной скорости

## 📁 Структура проекта

```
kampus-vpn/
├── README.md                    # Документация
└── app/                         # Wails приложение
    ├── main.go                  # Точка входа
    ├── app.go                   # Основная логика VPN
    ├── config_builder.go        # Генерация config.json
    ├── subscription.go          # Парсинг подписок
    ├── wireguard.go             # WireGuard конфиги
    ├── frontend/                # UI (HTML/CSS/JS)
    │   └── index.html
    └── build/                   # Сборка
        ├── bin/                 # Исполняемые файлы
        │   ├── KampusVPN.exe
        │   ├── bin/sing-box.exe
        │   └── template.json
        └── appicon.png
```

## 🚀 Сборка

### Требования
- Go 1.21+
- Wails CLI v2.9+
- Node.js 18+

### Windows

```bash
cd app
wails build
```

Результат: `app/build/bin/KampusVPN.exe`

### macOS / Linux

```bash
cd app
wails build
```

## 📦 Файлы рядом с приложением

При запуске приложение создаёт файлы в директории с exe:

| Файл | Описание |
|------|----------|
| `template.json` | Шаблон конфигурации sing-box |
| `config.json` | Сгенерированная конфигурация |
| `user_settings.json` | Настройки пользователя |
| `cache.db` | Кэш sing-box |
| `vpn.log` | Логи VPN |
| `bin/sing-box.exe` | Ядро sing-box |

## 🔧 Использование

### Добавление VPN-подписки

1. Нажмите на бейдж "Нет подписки"
2. Вставьте URL подписки или прямую ссылку (vless://, trojan://)
3. Нажмите "Проверить" → "Сохранить"

### Добавление рабочей сети (WireGuard)

1. Нажмите на бейдж "🏢 Рабочие сети"
2. Нажмите "➕ Добавить"
3. Вставьте стандартный WireGuard конфиг:
   ```ini
   [Interface]
   PrivateKey = ...
   Address = 10.0.0.2/32
   DNS = 192.168.1.1

   [Peer]
   PublicKey = ...
   AllowedIPs = 192.168.0.0/16
   Endpoint = vpn.company.com:51820
   PersistentKeepalive = 25
   ```
4. Укажите тег (латиница) и название
5. Нажмите "Проверить" → "Сохранить"

### Запуск VPN

Нажмите большую кнопку питания. При активном VPN:
- Кнопка светится зелёным
- Статус показывает "Подключено"

## 🛠️ Разработка

### Режим разработки

```bash
cd app
wails dev
```

### Структура кода

| Файл | Описание |
|------|----------|
| `app.go` | Start/Stop VPN, API для frontend |
| `config_builder.go` | Генерация config.json из template |
| `subscription.go` | Парсинг VLESS/Trojan/SS/VMess |
| `wireguard.go` | Парсинг WireGuard конфигов |

## 📝 Лицензия

MIT License © 2024-2025 Kampus VPN
